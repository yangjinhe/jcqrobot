package com.yjh.cqa.controller;

/**
 * Created by yangjh on 2019/7/22.
 */
public class IndexController {

    /*@GetRoute("/")
    @JSON
    public Object index() {
        return RspResult.getSuccessResult().setMsg("It Works!");
    }

    @GetRoute("/test")
    @JSON
    public Object test(@Param String test) {
        return RspResult.getSuccessResult().setMsg(test);
    }



    @GetRoute("/sendPrivateMsg")
    @JSON
    public Object sendPrivateMsg(@Param long qq, @Param String message) {
        int i = App.CQ.sendPrivateMsg(qq, message);
        if (i > 0) {
            return RspResult.getSuccessResult().setCode(i + "");
        } else {
            return RspResult.getFailtResult("发送失败").setCode(i + "");
        }

    }

    @GetRoute("/sendGroupMsg")
    @JSON
    public Object sendGroupMsg(@Param long groupId, @Param String message) {
        int i = App.CQ.sendGroupMsg(groupId, message);
        if (i > 0) {
            return RspResult.getSuccessResult().setCode(i + "");
        } else {
            return RspResult.getFailtResult("发送失败").setCode(i + "");
        }
    }

    @GetRoute("/sendDisCussMsg")
    @JSON
    public Object sendDiscussMsg(@Param long groupId, @Param String message) {
        int i = App.CQ.sendDiscussMsg(groupId, message);
        if (i > 0) {
            return RspResult.getSuccessResult().setCode(i + "");
        } else {
            return RspResult.getFailtResult("发送失败").setCode(i + "");
        }
    }

    @GetRoute("/deleteMsg")
    @JSON
    public Object deleteMsg(@Param long messageId) {
        int i = App.CQ.deleteMsg(messageId);
        if (i > 0) {
            return RspResult.getSuccessResult().setCode(i + "");
        } else {
            return RspResult.getFailtResult("发送失败").setCode(i + "");
        }
    }*/

}
